# Kilosort4 全部可调参数详解

> 基于仓库 `kilosort/parameters.py`、`kilosort/run_kilosort.py` 及相关源码分析整理。

---

## 一、主参数（MAIN_PARAMETERS）

这些参数在 GUI 主界面显示，通常是用户需要首先关注的参数。

### 1. `n_chan_bin` — 通道总数
| 属性 | 值 |
|------|-----|
| **默认值** | `None`（**必须由用户指定**） |
| **类型** | `int` |
| **取值范围** | `> 0` |
| **所属步骤** | data |

**含义**：二进制文件中存储的总通道数，可能与实际包含电生理数据的通道数不同。这是唯一一个必须由用户指定的参数，否则 `run_kilosort` 会抛出 `ValueError`。

**推荐值**：根据实际数据文件确定。对于 Neuropixels 1.0 探针，通常为 385（384 个记录通道 + 1 个同步通道）。

---

### 2. `fs` — 采样频率
| 属性 | 值 |
|------|-----|
| **默认值** | `30000` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | data |

**含义**：探针的采样频率（Hz）。

**推荐值**：通常为 30000 Hz（Neuropixels 默认值），根据实际采集设备设置。

---

### 3. `batch_size` — 批处理大小
| 属性 | 值 |
|------|-----|
| **默认值** | `60000` |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | data |

**含义**：每次批处理包含的采样点数。较大的批量会消耗更多 GPU 内存但可能更快，较小的批量适合 GPU 内存受限的情况。

**推荐值**：保持默认 60000（2秒数据@30kHz）。GPU 内存不足时可适当减小，如 30000。

---

### 4. `nblocks` — 漂移校正块数
| 属性 | 值 |
|------|-----|
| **默认值** | `1` |
| **类型** | `int` |
| **取值范围** | `≥ 0` |
| **所属步骤** | preprocessing |

**含义**：用于漂移校正的非重叠块数量（实际会产生 `2*nblocks-1` 个包含重叠的块）。设为 0 可跳过漂移校正。

**推荐值**：对于较长记录（>30分钟），建议设为 `5`；短记录可保持默认 `1`。增加块数可追踪更精细的时间漂移，但会增加计算时间。

---

### 5. `Th_universal` — 通用模板检测阈值
| 属性 | 值 |
|------|-----|
| **默认值** | `9` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | spike detection |

**含义**：通用模板的 spike 检测阈值（对应旧版 Kilosort 中的 Th(1)）。值越高越严格（检出更少的 spike），值越低越宽松（检出更多但可能包含更多噪声）。

**推荐值**：默认 `9`。高密度探针（如 Neuropixels）数据质量较好时可用 `[8, 10]` 范围；低信噪比数据可降低到 `7-8`；极高信噪比可提高到 `10-12`。

---

### 6. `Th_learned` — 学习模板检测阈值
| 属性 | 值 |
|------|-----|
| **默认值** | `8` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | spike detection |

**含义**：学习到的模板的 spike 检测阈值（对应旧版 Kilosort 中的 Th(2)）。在第二阶段的模板匹配中使用，通常比 `Th_universal` 略低。

**推荐值**：默认 `8`。一般比 `Th_universal` 低 0.5-1。若要检测更多的 spike 以增加 cluster 完整性，可降低到 `7`。

---

### 7. `tmin` — 数据起始时间
| 属性 | 值 |
|------|-----|
| **默认值** | `0` |
| **类型** | `float` |
| **取值范围** | `≥ 0` |
| **所属步骤** | data |

**含义**：开始排序的数据起始时间（秒）。用于跳过记录开头的伪迹或不稳定部分。

**推荐值**：默认 `0`。如果有开头伪迹，可能设为 `5-30` 秒。

---

### 8. `tmax` — 数据结束时间
| 属性 | 值 |
|------|-----|
| **默认值** | `inf` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | data |

**含义**：排序数据结束时间（秒）。默认处理到记录末尾。用于仅处理数据的一个子段。

**推荐值**：默认 `inf`。仅用于选择性分析部分数据。

---

## 二、扩展参数（EXTRA_PARAMETERS）

这些参数在 GUI 的 "Extra settings" 窗口中显示，分为数据、预处理、spike 检测、聚类和后处理五个步骤。

### 数据参数（Data）

### 9. `nt` — 波形采样点数
| 属性 | 值 |
|------|-----|
| **默认值** | `61` |
| **类型** | `int`（必须为奇数） |
| **取值范围** | `≥ 1` |
| **所属步骤** | data |

**含义**：每个 spike 波形的采样点数和滤波的对称填充大小。必须为奇数。

**推荐值**：默认 `61`（约 2ms @ 30kHz）。增加该值可捕获更长的波形但会增加计算量。使用预定义模板（`templates_from_data=False`）时，必须为 `61`。

---

### 10. `shift` — 数据偏移
| 属性 | 值 |
|------|-----|
| **默认值** | `None` |
| **类型** | `float` |
| **取值范围** | 无限制 |
| **所属步骤** | data |

**含义**：在所有其他操作前对数据施加的标量偏移。通常不需要设置，但对 float32 数据可能需要。与 `scale` 配合使数据大致落在 `[-100, +100]` 范围内。

**推荐值**：默认 `None`。仅在数据格式异常时使用（如 float32 数据有较大 DC 偏移时）。

---

### 11. `scale` — 数据缩放
| 属性 | 值 |
|------|-----|
| **默认值** | `None` |
| **类型** | `float` |
| **取值范围** | 无限制 |
| **所属步骤** | data |

**含义**：在所有其他操作前对数据施加的缩放因子。通常不需要设置，但对 float32 数据可能需要。数据变换为 `data = data*scale + shift`。

**推荐值**：默认 `None`。仅在数据格式异常时使用。

---

### 12. `batch_downsampling` — 批下采样因子
| 属性 | 值 |
|------|-----|
| **默认值** | `1` |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | data |

**含义**：每 N 个 batch 中只用 1 个。如设 `batch_downsampling=10`，仅每第 10 个 batch 用于排序。

**推荐值**：默认 `1`（使用所有数据批次）。通常不应修改，仅在快速测试或资源受限时使用。

---

### 预处理参数（Preprocessing）

### 13. `artifact_threshold` — 伪迹阈值
| 属性 | 值 |
|------|-----|
| **默认值** | `inf` |
| **类型** | `float` |
| **取值范围** | `≥ 0` |
| **所属步骤** | preprocessing |

**含义**：若某个 batch 包含绝对值高于此阈值的数据，则将该 batch 归零（假设存在记录伪迹）。默认无穷大意味着不做任何归零。

**推荐值**：默认 `inf`。如果数据有较大电噪声或运动伪迹，可设为合理值如 `500-1000`。大多数情况下应保持默认，避免误杀信号。

---

### 14. `nskip` — 白化矩阵批次步长
| 属性 | 值 |
|------|-----|
| **默认值** | `25` |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | preprocessing |

**含义**：计算白化矩阵时跳过的批次步长。每隔 `nskip` 个批次采集一次数据来计算协方差矩阵。

**推荐值**：默认 `25`。对于大多数数据无需修改。

---

### 15. `whitening_range` — 白化邻域范围
| 属性 | 值 |
|------|-----|
| **默认值** | `32` |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | preprocessing |

**含义**：估计每个通道的白化矩阵时，使用的相邻通道数量。

**推荐值**：默认 `32`。对于高密度探针来说是最优值；对于通道数较少的探针，系统会自动调整到不超过通道总数。

---

### 16. `highpass_cutoff` — 高通滤波截止频率
| 属性 | 值 |
|------|-----|
| **默认值** | `300` |
| **类型** | `float` |
| **取值范围** | `≥ 0` |
| **所属步骤** | preprocessing |

**含义**：应用于数据的 Butterworth 高通滤波器的临界频率（Hz）。

**推荐值**：默认 `300` Hz。适用于大多数 spike 数据。如果 LFP 频段有强干扰，可提高到 `400-500` Hz。降低可能保留更多低频信号但也可能引入漂移伪迹。

---

### 17. `binning_depth` — 漂移直方图垂直分箱
| 属性 | 值 |
|------|-----|
| **默认值** | `5` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | preprocessing |

**含义**：漂移校正中用于 2D 直方图的垂直分箱大小（微米）。

**推荐值**：默认 `5` μm。更小的值提供更高的空间分辨率但可能使漂移估计对噪声更敏感。

---

### 18. `sig_interp` — 空间平滑尺度
| 属性 | 值 |
|------|-----|
| **默认值** | `20` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | preprocessing |

**含义**：漂移校正的近似空间平滑尺度（微米）。控制用于插值的径向核的宽度。

**推荐值**：默认 `20` μm。与通道间距相关，对于典型 Neuropixels 探针（~20μm 间距），此值是合适的。减小该值可使插值更局部化，增大则更平滑。

---

### 19. `drift_smoothing` — 漂移估计平滑系数
| 属性 | 值 |
|------|-----|
| **默认值** | `[0.5, 0.5, 0.5]` |
| **类型** | `list`（3 个 float） |
| **取值范围** | 无硬限制 |
| **所属步骤** | preprocessing |

**含义**：对时空漂移估计施加的高斯平滑量，分别对应三个轴：相关性轴（建议不调整）、时间轴（以配准块为单位）、y 轴（以批次为单位）。当 `nblocks=1` 时，y 方向的平滑没有效果。

**推荐值**：保持默认 `[0.5, 0.5, 0.5]`。不建议调整相关性轴的平滑。若漂移估计看起来过于颠簸，可适当提高平滑值。

---

### Spike 检测参数（Spike Detection）

### 20. `nt0min` — 波形对齐点
| 属性 | 值 |
|------|-----|
| **默认值** | `None`（自动设为 `int(20*nt/61)`） |
| **类型** | `int` |
| **取值范围** | `≥ 0` |
| **所属步骤** | spike detection |

**含义**：波形对齐的样本索引，使波形的最小值或最大值出现在此位置。当 `nt=61` 时，默认值为 20（约在波形 1/3 处）。

**推荐值**：默认自动计算。一般不需要手动调整。

---

### 21. `dmin` — 垂直模板间距
| 属性 | 值 |
|------|-----|
| **默认值** | `None`（自动确定） |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | spike detection |

**含义**：用于 spike 检测的模板中心的垂直间距（微米）。当为 `None` 时，根据探针接触点的中位间距自动确定。

**推荐值**：默认自动即可。如果自动值不合适，可手动设定为探针垂直间距的 1-2 倍。对于 Neuropixels 1.0（~20μm 间距），自动值通常约为 `20`。

---

### 22. `dminx` — 水平模板间距
| 属性 | 值 |
|------|-----|
| **默认值** | `32` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | spike detection |

**含义**：用于 spike 检测的模板中心的水平间距（微米）。默认 32μm 对 Neuropixels 1 和 Neuropixels 2 探针效果良好。

**推荐值**：Neuropixels 1/2 保持 `32`。对于其他探针几何形状，建议先设为探针接触点的中位水平距离。如果检测到的 cluster 太少，可以减小该值以放置更多模板。

---

### 23. `min_template_size` — 最小模板大小
| 属性 | 值 |
|------|-----|
| **默认值** | `10` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | spike detection |

**含义**：通用模板的最小球形空间包络的高斯标准差。

**推荐值**：默认 `10`。对于大多数探针是合适的。如果观察到大振幅的神经元数量较少，可降低到 `5`；如果主要关注小振幅神经元，可适当提高。

---

### 24. `template_sizes` — 模板尺寸数量
| 属性 | 值 |
|------|-----|
| **默认值** | `5` |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | spike detection |

**含义**：通用模板的尺寸变体数量（`min_template_size` 的倍数）。更多尺寸可检测更多空间范围不同的神经元，但会增加计算量。

**推荐值**：默认 `5`。对于大多数数据已足够。如果检测到多种空间范围的神经元（如同时有轴突和胞体信号），可增加到 `6-7`。

---

### 25. `nearest_chans` — 最近通道数
| 属性 | 值 |
|------|-----|
| **默认值** | `10` |
| **类型** | `int` |
| **取值范围** | `≥ 1`（不超过数据通道总数） |
| **所属步骤** | spike detection |

**含义**：在 spike 检测中寻找局部最大值时，考虑的最近通道数量。与探针几何相关：该值应确保足够多的通道出现来验证 spike。

**推荐值**：默认 `10`。对于 Neuropixels 高密度探针效果良好。对于线性探针可以保持此值；对于 tetrodes 这种少通道配置，系统会自动调整。

---

### 26. `nearest_templates` — 最近模板数
| 属性 | 值 |
|------|-----|
| **默认值** | `100` |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | spike detection |

**含义**：在 spike 检测中寻找局部最大值时，考虑的最近模板位置数量。

**推荐值**：默认 `100`。保持默认值即可。

---

### 27. `max_channel_distance` — 最大通道距离
| 属性 | 值 |
|------|-----|
| **默认值** | `32` |
| **类型** | `float` |
| **取值范围** | `≥ 1` |
| **所属步骤** | spike detection |

**含义**：距离其最近通道超过此值（微米）的模板将不会被使用。同时也限制了聚类期间比较通道之间的距离。

**推荐值**：默认 `32` μm。减小可加速计算但可能遗漏远离通道位置的信号；增大可包含更多空间上远离的模板。

---

### 28. `max_peels` — 匹配追踪迭代次数
| 属性 | 值 |
|------|-----|
| **默认值** | `100` |
| **类型** | `int` |
| **取值范围** | `1` ~ `10000` |
| **所属步骤** | spike detection |

**含义**：在匹配追踪步骤中，对每批数据进行的最大迭代次数。更多的迭代可以检测更多重叠的 spike。

**推荐值**：默认 `100`。对于高发放率或密集放电区域，可增加到 `150-200` 以更好地分离重叠 spike。降低可加快排序但可能漏检重叠 spike。

---

### 29. `templates_from_data` — 从数据估计模板
| 属性 | 值 |
|------|-----|
| **默认值** | `True` |
| **类型** | `bool` |
| **取值范围** | `True` 或 `False` |
| **所属步骤** | spike detection |

**含义**：是否从数据中估计通用模板所用的 spike 形状。如果设为 `False`，则使用内置的预定义模板（此时 `nt` 必须为 `61`）。

**推荐值**：默认 `True`。使用数据驱动的模板几乎总是更好的选择。仅在快速测试或数据异常时可设为 `False`。

---

### 30. `n_templates` — 单通道模板数
| 属性 | 值 |
|------|-----|
| **默认值** | `6` |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | spike detection |

**含义**：用于构建通用模板的单通道模板数量（仅在 `templates_from_data=True` 时使用）。使用 K-Means 聚类从数据中提取的典型 spike 形状数。

**推荐值**：默认 `6`。一般足够覆盖单通道的所有波形形状。增加可捕获更稀有的波形形状，但影响较小。

---

### 31. `n_pcs` — PC 特征数
| 属性 | 值 |
|------|-----|
| **默认值** | `6` |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | spike detection |

**含义**：提取 spike 特征时使用的单通道主成分（PC）数量（仅在 `templates_from_data=True` 时使用）。

**推荐值**：默认 `6`。对于大多数场景，3-6 个 PCs 足以捕获 >90% 的波形方差。增加会提高特征维度但可能引入噪声成分；减少可能丢失波形细节。

---

### 32. `Th_single_ch` — 单通道阈值
| 属性 | 值 |
|------|-----|
| **默认值** | `6` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | spike detection |

**含义**：用于计算通用模板的单通道阈值穿越检测（以白化数据的标准差为单位）。

**推荐值**：默认 `6`。较低值（`4-5`）可检测更多小振幅 spike 用于模板构建，但可能引入噪声；较高值（`7-8`）更保守。

---

### 聚类参数（Clustering）

### 33. `acg_threshold` — ACG 不应期阈值
| 属性 | 值 |
|------|-----|
| **默认值** | `0.2` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | clustering |

**含义**：在 ACG（自相关图）中允许的、相对于基线的不应期违规比例。用于判定 cluster 是否为 "good" unit。

**推荐值**：默认 `0.2`（20% 的不应期违规容忍度）。较严格设 `0.1`（更可能判定为噪声）；较宽松设 `0.3-0.5`。严格设置会产生更"纯"的 unit 但可能丢失一些真实的 spike；宽松设置保留更多 spike 但可能包含更多污染。

---

### 34. `ccg_threshold` — CCG 不应期阈值
| 属性 | 值 |
|------|-----|
| **默认值** | `0.25` |
| **类型** | `float` |
| **取值范围** | `> 0` |
| **所属步骤** | clustering |

**含义**：在 CCG（互相关图）中允许的、相对于基线的不应期违规比例。用于执行 cluster 的 split 和 merge 操作。

**推荐值**：默认 `0.25`。此参数在合并相似 cluster 时起着关键的决策作用。较高的值会导致更多合并（过度合并），较低的值会导致更多分裂（过度分裂）。

---

### 35. `cluster_neighbors` — 聚类最近邻数
| 属性 | 值 |
|------|-----|
| **默认值** | `10` |
| **类型** | `int` |
| **取值范围** | `≥ 2` |
| **所属步骤** | clustering |

**含义**：在 `clustering_qr.neigh_mat` 中为每个 spike 搜索的最近邻数量，用于构建聚类图的邻接矩阵。更改此参数会影响资源使用和排序时间。

**推荐值**：默认 `10`。增大可改善聚类精度但会增加内存使用和计算时间。对于非常大的数据集，可考虑减小到 `5-7` 以节省资源。

---

### 36. `cluster_downsampling` — 聚类下采样因子
| 属性 | 值 |
|------|-----|
| **默认值** | `20` |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | clustering |

**含义**：聚类中用于地标（landmark）的 spike 比例的倒数。默认每 20 个 spike 使用 1 个作为地标。实际使用的 spike 数量还受 `max_cluster_subset` 的上限控制。

**推荐值**：默认 `20`。对于大多数数据 `5-20` 之间的值都能良好工作。设为 `1` 表示使用所有 spike（最精确但最慢、最耗内存）。当遇到内存问题时，系统自动退回到 `20` 的默认值。

---

### 37. `max_cluster_subset` — 聚类最大子集
| 属性 | 值 |
|------|-----|
| **默认值** | `25000` |
| **类型** | `int` 或 `None` |
| **取值范围** | `≥ 1`（`None` = 无限制） |
| **所属步骤** | clustering |

**含义**：聚类中用于搜索最近邻的最大 spike 数量。防止对于极长记录，聚类子集无限制增长。设为 `None` 表示不设上限（旧版行为）。

**推荐值**：默认 `25000`。对于大多数情况足够。若聚类结果似乎过粗糙，可增加到 `50000`；若内存紧张可减小到 `10000`。设为 `None` 使用所有可用 spike（不推荐，大多数情况下不必要且会造成性能瓶颈）。

---

### 38. `x_centers` — X 方向聚类中心数
| 属性 | 值 |
|------|-----|
| **默认值** | `None`（自动检测） |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | clustering |

**含义**：为模板分组确定中心点时，使用的 x 位置数量。当设为 `None` 时，通过检测通道密度的峰值来自动确定。

**推荐值**：对于 Neuropixels 1/2 等单列探针，自动检测即可（通常为 1）。**对于 2D 阵列探针，建议手动指定**，每隔几百微米设置一个中心。例如，一个 4 列探针可以设 `x_centers=4`。

---

### 39. `cluster_init_seed` — 聚类初始化随机种子
| 属性 | 值 |
|------|-----|
| **默认值** | `5` |
| **类型** | `int` |
| **取值范围** | `≥ 1` |
| **所属步骤** | clustering |

**含义**：用于聚类图初始化的 kmeans++ 算法的随机种子。

**推荐值**：默认 `5`。一般无需修改。若要确保可重复性，可固定为任意值。

---

### 后处理参数（Postprocessing）

### 40. `duplicate_spike_ms` — 重复 spike 去除窗口
| 属性 | 值 |
|------|-----|
| **默认值** | `0.25` |
| **类型** | `float` |
| **取值范围** | `≥ 0`（设为 `0` 禁用此步骤） |
| **所属步骤** | postprocessing |

**含义**：对于同一 cluster，在此时间窗口（毫秒）内出现的后续 spike 被假定为伪迹而移除。默认值 0.25ms 等价于旧版参数 `duplicate_spike_bins=7`（@30kHz）。

**推荐值**：默认 `0.25` ms。对应于典型的不应期窗口。若要保留所有 spike，可设为 `0`。此参数替代了已弃用的 `duplicate_spike_bins`。

---

### 41. `position_limit` — 位置估计最大距离
| 属性 | 值 |
|------|-----|
| **默认值** | `100` |
| **类型** | `float` |
| **取值范围** | `≥ 0` |
| **所属步骤** | postprocessing |

**含义**：在 `postprocessing.compute_spike_positions` 中用于估计 spike 位置的通道间最大距离（微米）。不影响 spike 排序本身，仅影响排序后位置估计方式。

**推荐值**：默认 `100` μm。较小的值使位置估计更局部，较大值则使用更广泛的通道来计算位置。

---

## 三、其他运行时参数（run_kilosort 函数参数）

这些参数不通过 `settings` 字典传入，而是作为 `run_kilosort()` 的直接参数。

### 42. `do_CAR` — 共平均参考
| 默认值 | `True` |
|------|--------|

**含义**：是否在预处理中应用共平均参考（Common Average Reference）。这是一个减除所有通道均值的操作，可有效去除共模噪声。

**推荐值**：默认 `True`。强烈推荐保持开启，除非有特定原因（如通道数很少的 tetrode 数据）。

---

### 43. `invert_sign` — 反转信号
| 默认值 | `False` |
|------|--------|

**含义**：是否翻转数据的正负值以符合 Kilosort4 期望的标准。

**推荐值**：默认 `False`。不推荐修改，除非确认数据使用了相反的符号约定。

---

### 44. `clear_cache` — 清除 GPU 缓存
| 默认值 | `False` |
|------|--------|

**含义**：是否在内存密集型操作之间强制 PyTorch 释放 GPU 缓存内存。

**推荐值**：默认 `False`。**不推荐设为 `True`**，除非遇到 GPU 内存不足错误（OOM），因为它会导致排序变慢。

---

### 45. `save_preprocessed_copy` — 保存预处理数据
| 默认值 | `False` |
|------|--------|

**含义**：是否将经过预处理（白化、滤波、漂移校正）的数据副本保存到 `temp_wh.dat`，并相应修改 Phy 输出格式。

**推荐值**：默认 `False`。仅在需要在 Phy 中手动策展并且偏好使用预处理后的数据时开启。会占用额外磁盘空间。

---

### 46. `verbose_log` — 详细日志
| 默认值 | `False` |
|------|--------|

**含义**：是否在日志中包含额外的调试级信息。提供更多细节用于调试，但可能影响性能。

**推荐值**：默认 `False`。仅在调试问题时开启。

---

### 47. `data_dtype` — 数据类型
| 默认值 | `'int16'` |
|------|--------|

**含义**：二进制文件中数据的 dtype。

**推荐值**：默认 `'int16'`（大多数采集系统使用）。如果数据以其他格式存储（如 `'uint16'`、`'float32'`），必须指定此参数。

---

### 48. `device` — PyTorch 计算设备
| 默认值 | `None`（自动选择 GPU（若可用），否则 CPU） |
|------|--------|

**含义**：用于 PyTorch 计算的计算设备（CPU 或 GPU）。

**推荐值**：默认。有 NVIDIA GPU 时强烈推荐使用 GPU（极大加速）；无 GPU 时使用 CPU。

---

### 49. `torch_thread_lim` — PyTorch 线程限制
| 默认值 | `None`（不限制） |
|------|--------|

**含义**：限制 PyTorch 在 CPU 上使用的线程数。参见 `torch.set_num_threads`。

**推荐值**：默认 `None`。仅在多核 CPU 上需要控制资源使用时设置。

---

### 50. `bad_channels` — 排除通道
| 默认值 | `None`（不排除任何通道） |
|------|--------|

**含义**：应从排序中排除的通道索引列表（二进制文件中的行号）。在列表中的通道等效于从探针字典中移除。

**推荐值**：默认 `None`。仅在有已知坏通道（如高噪声、无信号）时指定。

---

### 51. `verbose_console` — 控制台详细输出
| 默认值 | `False` |
|------|--------|

**含义**：是否将控制台日志级别设为 `DEBUG` 而非 `INFO`，使通常仅写入日志文件的额外信息实时显示。

**推荐值**：默认 `False`。仅在需要实时监控排序详细过程时开启。

---

## 四、参数速查表

| 参数 | 默认值 | 类别 | 影响范围 |
|------|--------|------|----------|
| `n_chan_bin` | **必填** | data | 数据读取 |
| `fs` | 30000 | data | 全局时间计算 |
| `batch_size` | 60000 | data | GPU 内存 / 速度 |
| `nblocks` | 1 | preprocessing | 漂移校正精度 |
| `Th_universal` | 9 | spike detection | 第一阶段 spike 检出数 |
| `Th_learned` | 8 | spike detection | 第二阶段 spike 检出数 |
| `tmin` | 0 | data | 分析时间范围 |
| `tmax` | inf | data | 分析时间范围 |
| `nt` | 61 | data | 波形长度 |
| `nt0min` | auto | spike detection | 波形对齐 |
| `dmin` | auto | spike detection | 模板空间覆盖 |
| `dminx` | 32 | spike detection | 模板水平间距 |
| `min_template_size` | 10 | spike detection | 模板空间大小 |
| `template_sizes` | 5 | spike detection | 模板数量 |
| `nearest_chans` | 10 | spike detection | 通道邻域大小 |
| `nearest_templates` | 100 | spike detection | 模板邻域大小 |
| `max_channel_distance` | 32 | spike detection | 模板-通道距离限制 |
| `max_peels` | 100 | spike detection | 重叠 spike 检测 |
| `Th_single_ch` | 6 | spike detection | 单通道模板检测 |
| `n_pcs` | 6 | spike detection | 特征维度 |
| `n_templates` | 6 | spike detection | 单通道模板数 |
| `templates_from_data` | True | spike detection | 模板来源 |
| `acg_threshold` | 0.2 | clustering | Good unit 判定 |
| `ccg_threshold` | 0.25 | clustering | Split/merge 判定 |
| `cluster_neighbors` | 10 | clustering | 聚类图构建 |
| `cluster_downsampling` | 20 | clustering | 聚类 spike 子采样 |
| `max_cluster_subset` | 25000 | clustering | 聚类内存上限 |
| `x_centers` | auto | clustering | 模板分组 |
| `cluster_init_seed` | 5 | clustering | 聚类可重复性 |
| `duplicate_spike_ms` | 0.25 | postprocessing | 重复 spike 过滤 |
| `position_limit` | 100 | postprocessing | Spike 位置估计 |
| `artifact_threshold` | inf | preprocessing | 伪迹检测 |
| `nskip` | 25 | preprocessing | 白化矩阵计算 |
| `whitening_range` | 32 | preprocessing | 白化邻域 |
| `highpass_cutoff` | 300 | preprocessing | 滤波 |
| `binning_depth` | 5 | preprocessing | 漂移分箱精度 |
| `sig_interp` | 20 | preprocessing | 插值平滑 |
| `drift_smoothing` | [0.5,0.5,0.5] | preprocessing | 漂移估计平滑 |
| `shift` | None | data | 数据归一化 |
| `scale` | None | data | 数据归一化 |
| `batch_downsampling` | 1 | data | 数据下采样 |
| `do_CAR` | True | 运行时 | 共模噪声去除 |
| `invert_sign` | False | 运行时 | 信号极性 |
| `clear_cache` | False | 运行时 | GPU 内存管理 |
| `save_preprocessed_copy` | False | 运行时 | 数据导出选项 |
| `verbose_log` | False | 运行时 | 日志详细程度 |
| `data_dtype` | 'int16' | 运行时 | 数据读取 |
| `device` | auto | 运行时 | 计算设备选择 |
| `torch_thread_lim` | None | 运行时 | CPU 线程控制 |
| `bad_channels` | None | 运行时 | 通道排除 |
| `verbose_console` | False | 运行时 | 控制台输出级别 |

---

## 五、关键调参建议

1. **首先关注 `n_chan_bin` 和探针配置**：确保通道数和探针布局正确是成功排序的前提。

2. **漂移校正最重要**：对于长记录（>30分钟），将 `nblocks` 从 1 增加到 5 可显著改善结果。对于急性记录（<10分钟），`nblocks=1` 通常足够。

3. **阈值调整**：`Th_universal` 和 `Th_learned` 控制检测灵敏度。默认值偏保守，若检出 spike 太少，可尝试降低 0.5-1。

4. **GPU 内存不足**：减小 `batch_size` 或设置 `clear_cache=True`（后者不推荐，会降低性能）。

5. **2D 阵列探针**：务必手动设置 `x_centers` 参数，自动检测对此类探针效果不佳。

6. **大部分参数保持默认即可**：Kilosort4 的默认参数已经过广泛测试，对于大多数标准记录（Neuropixels 探针、30kHz 采样率、int16 数据），默认值就能获得良好的排序结果。
